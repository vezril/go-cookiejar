package Tritw

import (
  "github.com/vezril/go-cookiejar/representation/trits"
)

type tritw struct {
  data  []Trits.trits
}

func New() *tritw {
  t = New(tritw)
  t.data = make([]Trits.trits, 10)

  return t
}
